{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "fiware-quantumleap.serviceAccountName" . }}
  labels:
{{ include "fiware-quantumleap.labels" . | nindent 4 }}
{{- end -}}
