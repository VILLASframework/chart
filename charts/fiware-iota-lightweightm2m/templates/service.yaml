apiVersion: v1
kind: Service
metadata:
  name: {{ include "fiware-iota-lightweightm2m.fullname" . }}
  labels:
    {{- include "fiware-iota-lightweightm2m.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fiware-iota-lightweightm2m.selectorLabels" . | nindent 4 }}
