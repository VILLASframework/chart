apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fiware-iota-lightweightm2m.fullname" . }}-test-connection"
  labels:
{{ include "fiware-iota-lightweightm2m.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "fiware-iota-lightweightm2m.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
