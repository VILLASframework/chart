apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fiware-keyrock.fullname" . }}-test-connection"
  labels:
{{ include "fiware-keyrock.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "fiware-keyrock.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
