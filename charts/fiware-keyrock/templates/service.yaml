apiVersion: v1
kind: Service
metadata:
  name: {{ include "fiware-keyrock.fullname" . }}
  labels:
    {{- include "fiware-keyrock.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fiware-keyrock.selectorLabels" . | nindent 4 }}
