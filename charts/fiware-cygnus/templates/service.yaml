apiVersion: v1
kind: Service
metadata:
  name: {{ include "fiware-cygnus.fullname" . }}
  labels:
    {{- include "fiware-cygnus.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fiware-cygnus.selectorLabels" . | nindent 4 }}
