apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fiware-cygnus.fullname" . }}-test-connection"
  labels:
{{ include "fiware-cygnus.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "fiware-cygnus.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
