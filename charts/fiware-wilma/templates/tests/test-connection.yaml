apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fiware-wilma.fullname" . }}-test-connection"
  labels:
{{ include "fiware-wilma.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "fiware-wilma.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
