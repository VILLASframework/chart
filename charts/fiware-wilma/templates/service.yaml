apiVersion: v1
kind: Service
metadata:
  name: {{ include "fiware-wilma.fullname" . }}
  labels:
    {{- include "fiware-wilma.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fiware-wilma.selectorLabels" . | nindent 4 }}
