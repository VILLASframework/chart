apiVersion: v1
kind: Service
metadata:
  name: {{ include "fiware-wirecloud.fullname" . }}
  labels:
    {{- include "fiware-wirecloud.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fiware-wirecloud.selectorLabels" . | nindent 4 }}
