{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "fiware-wirecloud.serviceAccountName" . }}
  labels:
{{ include "fiware-wirecloud.labels" . | nindent 4 }}
{{- end -}}
