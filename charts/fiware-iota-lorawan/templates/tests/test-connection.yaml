apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fiware-iota-lorawan.fullname" . }}-test-connection"
  labels:
{{ include "fiware-iota-lorawan.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "fiware-iota-lorawan.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
